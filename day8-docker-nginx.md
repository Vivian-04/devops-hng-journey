Day 8 - Docker with Nginx

- Ran nginx container using Docker
- Exposed container on port 80 and 8090
- Accessed nginx via browser using EC2 public IP
- Used docker logs and docker exec
- Understood image vs container
